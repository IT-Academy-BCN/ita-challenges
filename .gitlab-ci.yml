# Central pipeline: ita-cicd (push that repo to GitLab and set project path below).
variables:
  ITA_APPLICATIONS: "frontend,backend"
  ITA_BACKEND_SERVICES: "challenge,account"

include:
  - project: bcna/ita-cicd
    ref: main
    file: pipeline.yml
  - local: .gitlab/ci/sonarqube.yml
